# Terraform-specific files
# =========================

# Local Terraform directories containing modules and plugins.
# This is generated by 'terraform init' and should not be versioned.
.terraform/

# Local state files. These can contain sensitive data.
# In a team environment, always use a remote backend for state.
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local testing.
override.tf
override.tf.json
*.override.tf
*.override.tf.json

# CLI configuration files
.terraformrc
terraform.rc

# General OS-specific files
# =========================
.DS_Store
Thumbs.db